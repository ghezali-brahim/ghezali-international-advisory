# i18nexus — clé API pour récupérer les traductions (pull)
# Créer un projet sur https://app.i18nexus.com et récupérer la clé dans Settings.
# Ce projet utilise le namespace "common" uniquement (locales/{lang}/common.json).
# Dans i18nexus, configurer le projet en "i18next + Next.js App Router" et utiliser le namespace "common".
I18NEXUS_API_KEY=

# Optionnel : pour push de nouvelles clés, utiliser un Personal Access Token (PAT)
# créé depuis le dashboard i18nexus (écriture)
# I18NEXUS_PAT=

# Resend — envoi d’emails (formulaire contact). Créer un compte sur https://resend.com et récupérer la clé API.
RESEND_API_KEY=
# Optionnel : adresse d’envoi (ex. contact@ghezaliadvisory.com). Par défaut : onboarding@resend.dev pour les tests.
# RESEND_FROM=
